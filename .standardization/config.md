# Standardization configuration

Working path: .standardization
Publication path: standards/v1
Versioning system: Git
Published version convention: Preserve identifiable published versions in versioned publication directories using explicit document version metadata and matching Git tags
Closeout pruning protection: Git once committed; confirmation required for uncommitted material
